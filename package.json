{
  "name": "flux-monorepo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "dev": "pnpm --filter @flux/web dev",
    "dev:server": "pnpm --filter @flux/server dev",
    "build": "node scripts/build-meta.mjs",
    "preview": "pnpm --filter @flux/web preview",
    "typecheck": "pnpm -r build",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
